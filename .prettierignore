node_modules
out
dist
resources
package-lock.json
# Design mockups are point-in-time artifacts, not living code.
docs/mockups
# The NSIS setup kit keeps its own (Prism-derived) formatting.
build/installer
patches
