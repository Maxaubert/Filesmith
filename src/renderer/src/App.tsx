// Deliberately plain placeholder. The real UI is designed collaboratively
// (mockups first, then build) — see the project README / CLAUDE.md.
export default function App(): React.JSX.Element {
  return (
    <div className="grid min-h-screen place-items-center bg-neutral-950 text-neutral-100">
      <div className="space-y-2 text-center">
        <h1 className="text-2xl font-semibold tracking-tight">Filesmith</h1>
        <p className="text-sm text-neutral-500">
          Scaffold running. UI design is pending — nothing here is final.
        </p>
      </div>
    </div>
  )
}
